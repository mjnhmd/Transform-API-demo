apply plugin: 'groovy'
apply plugin: 'maven'

dependencies {
    implementation fileTree(dir: 'libs', include: ['*.jar'])
    implementation gradleApi()
    implementation localGroovy()
    implementation 'com.android.tools.build:gradle:3.4.2'
    implementation 'com.android.tools.build:transform-api:1.5.0'
    implementation 'commons-io:commons-io:2.5'
}



ext {
    // 从Github上clone下来的项目的本地地址
    GITHUB_REPO_PATH = "http://nexus.corp.youdao.com/nexus/content/repositories/luna-android-release/"
    // compile 'com.audienl:super_library:1.0.0'
    PUBLISH_GROUP_ID = 'com.youdao.luna'
    PUBLISH_ARTIFACT_ID = 'YDGradleTest'
    PUBLISH_VERSION = '0.0.2'
}

uploadArchives {
    repositories.mavenDeployer {
//        def deployPath = file(project.GITHUB_REPO_PATH)
        repository(url: project.GITHUB_REPO_PATH) {
            authentication(userName: "keupload", password: "100200300")
        }
        pom.project {
            groupId project.PUBLISH_GROUP_ID
            artifactId project.PUBLISH_ARTIFACT_ID
            version project.PUBLISH_VERSION
        }

    }
}

